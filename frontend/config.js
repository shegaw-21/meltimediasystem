/**
 * config.js — loaded before app.js in every HTML page.
 *
 * Set API_BASE to your backend URL when frontend is deployed separately on Vercel.
 * Leave empty string when frontend + backend run together from the same server.
 */
window.API_BASE = 'https://meltimediasystem-2.onrender.com';
